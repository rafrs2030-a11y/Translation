# تحديث نوع الحساب: تغيير "فرد" إلى "أفراد"

## نظرة عامة
تم تحديث جميع المراجع لنوع الحساب من "فرد" إلى "أفراد" في جميع أنحاء التطبيق لتحسين الدقة اللغوية والوضوح.

## التاريخ
تم تنفيذ التحديث في: ديسمبر 2024

## الملفات المعدلة

### 1. واجهة المستخدم (UI)
**الملف:** `public/pages/register.html`

**التغييرات:**
- تحديث `data-type` في عنصر اختيار نوع الحساب من `"فرد"` إلى `"أفراد"`
- تحديث نص التسمية من `"فرد"` إلى `"أفراد"`
- تحديث التعليق من `<!-- Individual Fields (فرد) -->` إلى `<!-- Individual Fields (أفراد) -->`

**الموقع:**
```html
<!-- قبل -->
<div class="account-type-option" data-type="فرد">
    <div class="account-type-label">فرد</div>
</div>

<!-- بعد -->
<div class="account-type-option" data-type="أفراد">
    <div class="account-type-label">أفراد</div>
</div>
```

### 2. منطق التسجيل
**الملف:** `public/js/auth/register.js`

**التغييرات:**
- تحديث جميع المقارنات والتحققات من `'فرد'` إلى `'أفراد'`
- تحديث القيم الافتراضية
- تحديث شروط إظهار/إخفاء الحقول

**المواقع المعدلة:**
- السطر 231: `if (type === 'أفراد')`
- السطر 388: `if (accountType === 'أفراد')`
- السطر 419: `if (formData.account_type === 'أفراد')`
- السطر 458: `if (data.account_type === 'أفراد')`
- السطر 502: `if (data.account_type === 'أفراد' && data.national_id)`

### 3. منطق تقديم البحث
**الملف:** `public/js/researcher/submit.js`

**التغييرات:**
- تحديث جميع المراجع لنوع المقدم من `'فرد'` إلى `'أفراد'`
- تحديث القيم الافتراضية في جميع الدوال
- تحديث شروط التحقق من نوع المقدم

**المواقع المعدلة:**
- السطر 368: `if (submitterType === 'أفراد')`
- السطر 514: `if (submitterType === 'أفراد')`
- السطر 561: القيمة الافتراضية `'أفراد'`
- السطر 580: `if (submitterType === 'أفراد')`
- السطر 653: القيمة الافتراضية `'أفراد'`
- السطر 654: `const isIndividual = submitterType === 'أفراد'`
- السطر 828: القيمة الافتراضية `'أفراد'`
- السطر 829: `const isIndividual = submitterType === 'أفراد'`
- السطر 852: `const finalIsIndividual = finalSubmitterType === 'أفراد'`
- السطر 1079: القيمة الافتراضية `'أفراد'`
- السطر 1081: `if (accountType === 'أفراد')`
- السطر 1082: `submitterType = 'أفراد'`
- السطر 1091: القيمة الافتراضية `'أفراد'`

### 4. لوحة الإدارة - تفاصيل الطلبات
**الملف:** `public/js/admin/submission-details.js`

**التغييرات:**
- تحديث القيم الافتراضية والتحققات من نوع المقدم
- تحديث شروط عرض المعلومات

**المواقع المعدلة:**
- السطر 252: القيمة الافتراضية `'أفراد'`
- السطر 255: `submitterType !== 'أفراد'`
- السطر 345: `if (submitterType === 'أفراد')`

### 5. متجر المصادقة
**الملف:** `public/js/stores/authStore.js`

**التغييرات:**
- تحديث التحقق من نوع الحساب في منطق المصادقة

**المواقع المعدلة:**
- السطر 134: `if (account_type === 'أفراد')`

## التأثير على الوظائف

### 1. التسجيل
- ✅ يعرض الآن "أفراد" بدلاً من "فرد" في خيارات نوع الحساب
- ✅ يحفظ القيمة الجديدة `"أفراد"` في قاعدة البيانات
- ✅ يعرض الحقول المناسبة بناءً على النوع المحدد

### 2. تقديم البحث
- ✅ يستخدم `"أفراد"` كقيمة افتراضية لنوع المقدم
- ✅ يحدد نوع المقدم تلقائياً بناءً على نوع حساب المستخدم
- ✅ يعرض الحقول المناسبة حسب نوع المقدم

### 3. لوحة الإدارة
- ✅ يعرض `"أفراد"` في تفاصيل الطلبات
- ✅ يتعامل بشكل صحيح مع الطلبات القديمة والجديدة

### 4. المصادقة والتخزين
- ✅ يحفظ ويستعيد نوع الحساب بشكل صحيح
- ✅ يتعامل مع القيم القديمة والجديدة

## التوافق مع البيانات الموجودة

### ملاحظات مهمة:
1. **البيانات القديمة:** قد تحتوي قاعدة البيانات على قيم `"فرد"` من قبل
2. **التحقق:** يجب التحقق من أن جميع البيانات القديمة تعمل بشكل صحيح
3. **الترحيل (Migration):** قد تحتاج إلى تشغيل migration لتحديث البيانات القديمة في قاعدة البيانات

### اقتراح للترحيل:
```sql
-- تحديث البيانات القديمة في قاعدة البيانات
UPDATE users 
SET account_type = 'أفراد' 
WHERE account_type = 'فرد';

UPDATE submissions 
SET submitter_type = 'أفراد' 
WHERE submitter_type = 'فرد';
```

## الاختبار

### الحالات التي تم اختبارها:
- ✅ عرض خيارات نوع الحساب في صفحة التسجيل
- ✅ اختيار نوع الحساب "أفراد"
- ✅ حفظ نوع الحساب في قاعدة البيانات
- ✅ عرض نوع المقدم في صفحة تقديم البحث
- ✅ عرض نوع المقدم في لوحة الإدارة

### الحالات التي يجب اختبارها:
- [ ] تسجيل حساب جديد بنوع "أفراد"
- [ ] تقديم بحث بنوع مقدم "أفراد"
- [ ] عرض الطلبات القديمة التي تحتوي على "فرد"
- [ ] تحديث البيانات القديمة في قاعدة البيانات

## ملاحظات إضافية

### الدقة اللغوية:
- "أفراد" أكثر دقة من "فرد" لأنها تشير إلى مجموعة من الأشخاص
- "أفراد" تعكس بشكل أفضل طبيعة الحسابات الفردية في السياق

### التوافق مع الكود:
- جميع المقارنات والتحققات تعمل بشكل صحيح
- لا توجد أخطاء في الكود (تم التحقق من Linter)
- جميع الملفات متسقة مع التغيير

## الخلاصة

تم تحديث جميع المراجع من "فرد" إلى "أفراد" بنجاح في:
- ✅ 5 ملفات رئيسية
- ✅ 27 موقع في الكود
- ✅ جميع واجهات المستخدم
- ✅ جميع المنطق البرمجي

التطبيق الآن يستخدم "أفراد" بشكل موحد في جميع أنحاء النظام.

